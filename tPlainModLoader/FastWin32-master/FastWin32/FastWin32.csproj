<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <ProjectGuid>{0B6AC808-3A51-4072-954F-D55DE4F0209B}</ProjectGuid>
    <OutputType>Library</OutputType>
    <RootNamespace>FastWin32</RootNamespace>
    <AssemblyName>FastWin32</AssemblyName>
    <TargetFramework>net472</TargetFramework>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <!-- 上游 AssemblyInfo.cs 的版本号含通配符(2.6.*), 与确定性编译不兼容, 保持上游原行为 -->
    <Deterministic>false</Deterministic>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <SignAssembly>true</SignAssembly>
    <AssemblyOriginatorKeyFile>FastWin32.snk</AssemblyOriginatorKeyFile>
    <DebugType Condition="'$(Configuration)' == 'Debug'">pdbonly</DebugType>
    <DebugType Condition="'$(Configuration)' == 'Release'">pdbonly</DebugType>
    <DocumentationFile Condition="'$(Configuration)' == 'Debug'">bin\Debug\FastWin32.xml</DocumentationFile>
    <DocumentationFile Condition="'$(Configuration)' == 'Release'">bin\Release\FastWin32.xml</DocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="System.Windows.Forms" />
  </ItemGroup>

</Project>
